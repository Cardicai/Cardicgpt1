import React from 'react';
import ReactDOM from 'react-dom/client';
import CardicGPT from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CardicGPT />
  </React.StrictMode>
);
